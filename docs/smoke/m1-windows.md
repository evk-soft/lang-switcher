# M1 (Windows): ручной smoke-чеклист

Здесь живёт то, что автотестами не проверяется: хуки, оверлей, трей, DPI. Файл наполняется
задачами 9–20 плана M1 и сводится задачей 21 в прогоняемый чеклист с замером NFR.

**Правило заполнения:** пункт без наблюдаемого доказательства в чеклист не берётся. У каждого
пункта — *что запустить*, *на что смотреть* и *что именно доказывает успех*. Незапущенный пункт
помечается «не проверялось», а не «ок»: скил `quality-gates` требует доказательств, а не
утверждений.

**Обновление 2026-09-08:** исправлены постоянная работа аудио и hover-таймер трея;
очередь логов уменьшена. Native audio/tray smoke и ресурсные замеры описаны в
[отчёте](../research/2026-09-08-idle-fixes.md). Последний прогон: 152 тест, CPU
0,0201% среднего и 0,1559% максимума интервала, WorkingSet 16,87 МиБ после 5 минут.
Ручная матрица ниже и порог 15 МБ остаются открытыми; исторические таблицы
2026-09-07 не переписаны новыми результатами.

**Ручной прогон пользователя 2026-09-08:** поведение бейджа/звука не прошло
приёмку, но отдельные 20 переключений были запущены уже после выхода приложения.
Для повторения подготовлен `scripts/manual-check.ps1`: постоянный бейдж,
раздельный аудиотест, проверки живости и временные метки этапов.
[Разбор журналов и ограничений](../research/2026-09-08-user-manual-check.md).

**Последующая проверка звука 2026-09-08:** исправлено завершение 90-мс сигнала,
когда счётчик устройства достигал только 64–70 мс. Теперь учитывается IAudioClock;
20/20 коротких RU/EN достигли полной длительности, native shutdown прошёл.
Пользователь услышал сравнительную серию с короткими и длинным сигналами;
подтверждение отдельной серии после исправления ещё ожидается.
[Эксперимент, регрессии и границы проверки](../research/2026-09-08-audio-completion.md).

## A. Старт: DPI-awareness и манифест (задача 9)

Проверяет предпосылку, на которой стоит вся геометрия оверлея (ADR-0005, ADR-0010): координаты
считаются физическими пикселями только пока процесс Per-Monitor-V2 aware.

| Что запустить | На что смотреть | Что доказывает |
|---|---|---|
| `cargo run -p switcher-app -- --data-dir target/dpi-smoke --run-for 5` | в `target/dpi-smoke/logs` отчёт `from_manifest=true per_monitor_v2=true`; нет предупреждения `DPI manifest did not provide Per-Monitor-V2` | манифест реально попал в PE-образ и подействовал |
| негативный контроль: временно переименовать `crates/switcher-app/lang-switcher.manifest`, пересобрать, запустить снова; обязательно вернуть файл и обновить mtime | `WARN … DPI manifest did not provide Per-Monitor-V2`, `from_manifest=false`, при этом `per_monitor_v2=true` | (1) предупреждение не потерялось до установки логгера; (2) рантайм-страховка применяется; (3) сборка не падает без манифеста |
| вернуть файл, `touch` его, запустить снова | `WARN` исчез, снова `from_manifest=true` | состояние восстановлено |

**Результат прогона от 2026-08-25** (Windows 11 Enterprise 10.0.26200, debug-сборка, rustc 1.96.1;
конфигурация мониторов для этого пункта роли не играет и не фиксировалась):

- ✅ обычный прогон: `from_manifest=true per_monitor_v2=true`, `WARN` нет.
- ✅ негативный контроль: `WARN … awareness=DPI_AWARENESS(0) applied=Ok(())`, `from_manifest=false`;
  линковка прошла (то есть отсутствующий манифест даёт предупреждение сборки, а не `LNK1327`).
- ✅ восстановление: `from_manifest=true`, `WARN` нет.

> **Грабли, обязательные к соблюдению при повторе.** `mv`/переименование **сохраняет mtime**, поэтому
> после возврата файла Cargo считает его неизменившимся, `build.rs` не перезапускается, и прогон
> продолжает показывать `from_manifest=false` — выглядит как «манифест сломался навсегда». Нужен
> `touch lang-switcher.manifest` (или `cargo clean -p switcher-app`) после восстановления. Это
> наблюдение стоило одного ложного вывода при первом прогоне.

## B. Раскладка, четыре класса окон (задача 12)

Код задач 12–13 реализован. Реальное чтение HKL и установку подписок проверяли.
**2026-09-08: доставка в отдельной Win32-fixture не прошла:** 20 смен собственного
HKL подтверждены, приложение не записало ни одного чтения в интервале.
Причина не установлена; [доказательства и следующий прогон](../research/2026-09-08-layout-delivery.md).
Остальные классы окон ещё требуют проверки. Процедура и диагностические команды —
в разделе E; историческая матрица приёмки приведена ниже.

## C. Оверлей: click-through, topmost, mixed-DPI (задача 10)

Что запускать: `cargo run -p switcher-windows --example overlay_smoke`. Пример устанавливает
Per-Monitor-V2 до создания окна (примеры манифеста не получают, ADR-0010), сначала оставляет
бейдж неподвижным до Enter, затем проводит его по диагонали виртуального рабочего стола
40 шагами по 150 мс. Следующие фазы проверяют `Fixed` на мониторе активного терминала и
скрытый простой; каждая ожидает Enter, фиксированной паузы 20 секунд больше нет.

**Исправление 2026-09-07:** во время ожидания Enter главный поток обрабатывает события
оверлея и сразу перерастеризует бейдж при смене DPI. Это подтверждено регрессионным тестом;
реальная смена масштаба через Параметры после исправления вручную **не проверялась**.

- [ ] При неподвижном `Fixed` изменить рабочую область панели задач/appbar: бейдж
      остаётся внутри новой `rcWork`. Обработчик `WM_SETTINGCHANGE` проверен тестом;
      визуальный результат изменения панели задач ещё не проверялся.
- [ ] Повторить click-through, фокус и mixed-DPI после аудита. Прежние наблюдения ниже
      относятся к прогонам 2026-08-25 и не заменяют повторную ручную проверку.

**Пункты, проверенные по логу** (прогон 2026-08-25, Windows 11 Enterprise 10.0.26200,
debug-сборка; конфигурация: два монитора, границы виртуального рабочего стола
`left=-2560 top=0 width=5120 height=1600`, левый экран 144 DPI = 150 %, правый 96 DPI = 100 %):

| Пункт | Наблюдение | Что доказано |
|---|---|---|
| окно создаётся и рисуется | ни одной строки об ошибке `UpdateLayeredWindow` / `SetWindowPos` за 40 шагов + 2 показа `Fixed` | layered-окно, DIB-секция и блит работают на обоих масштабах |
| смена монитора замечается **сравнением**, а не сообщением | на шаге 20 (`x` переходит из отрицательных в положительные) `dpi_here` меняется 144 → 96 и появляется ровно одна строка `OverlayScaleChanged dpi=96` | триггер ADR-0005 (`image.dpi != monitor.dpi`) работает и не зависит от `WM_DPICHANGED` |
| ровно одно событие на переход | `grep -c OverlayScaleChanged` = **1** на весь прогон | дедупликация по паре `(image_dpi, monitor_dpi)` держит канал чистым |
| цикл сходится за один шаг | 144 → бейдж 66×39 px; после события перерисовка в 44×26 px; далее до конца прогона **ни одного** нового события | утверждение ADR-0005 «цикл сходится за один шаг» — не декларация |
| одинаковый видимый размер на 100 % и 150 % | 66 px при 150 % против 44 px при 100 % = один и тот же кажущийся размер | зазор и бейдж масштабируются вместе (`scaled(44,144)=66`) |
| штатное завершение | `exit=0`, ни одного `warn` про утёкшие GDI-объекты из `Drop for Surface` | порядок освобождения (восстановить объект → `DeleteObject` → `DeleteDC` → `DestroyWindow`) не роняет и не течёт |

**Ответ на UNVERIFIED-вопрос из `docs/research/2026-08-25-m1-api-research/geometry.md`.**
`WM_DPICHANGED` (`msg=736`) **приходит** нашему окну `WS_EX_LAYERED | WS_EX_TRANSPARENT |
WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW` и приходит **при перемещении, которое делаем мы сами** —
причём *синхронно, реентрантно внутри вызова `SetWindowPos`*: строка от wndproc (`13:46:18.5488`)
стоит **раньше** строки отчёта о рассогласовании из того же `move_to` (`13:46:18.5495`).

Два следствия, оба уже учтены в коде:

1. Поведение от сообщения по-прежнему не зависит — и это хорошо, потому что сообщение приходит
   *после* того, как `move_to` уже посчитал факты монитора, то есть дизайн «ждать
   `WM_DPICHANGED`» всегда отставал бы на один кадр.
2. Реентрантность делает выбор thread-local флага (вместо состояния через `GWLP_USERDATA`)
   **требованием безопасности, а не удобством**: wndproc исполняется, пока `move_to` держит
   `&mut self`, так что доступ к тому же состоянию через сырой указатель был бы алиасингом
   живой мутабельной ссылки — UB на обычном пути, а не в углу.

**Пункты, требующие человека за клавиатурой** (автотест и лог их увидеть не могут; при отказе
любого из них правится ADR-0004, а не флаги наугад). Отмеченные — уже проверены, остальные ждут:

- [ ] клик по бейджу попадает в окно под ним (click-through), а не «съедается»;
- [ ] фокус не уходит с активного окна — заголовок остаётся активным, каретка продолжает мигать;
- [x] **угловой блок 8×8 прозрачен насквозь — подтверждено 2026-08-25, измерением по скриншоту.**
      Значит `ULW_ALPHA` с `AC_SRC_ALPHA` действительно читает попиксельную альфу, а не рисует
      непрозрачный прямоугольник.

      Скриншот разобран попиксельно (искали чистый синий `RGB(0,0,255)` и чистый красный
      `RGB(255,0,0)` — цвета тела и полосы), и все четыре числа сошлись с тем, что вычисляет код:

      | Что | Ожидание из кода | Измерено |
      |---|---|---|
      | размер бейджа на мониторе 96 DPI | `scaled(44,96)`×`scaled(26,96)` = 44×26 | **44×26** |
      | прозрачный угол | 8 строк, в каждой пропущено 8 столбцов | строки 0–7 начинаются со столбца **8**, строки 8–25 — с нуля |
      | доля красного | `stripe_from = 44 − 44/3 = 30` → 14×26 из 1080 | **0,337**, красный справа |
      | синих пикселей | 1080 − 364 | **716** |

      Через выкус видна буква терминала **своим цветом**, а не подкрашенная — то есть у полностью
      прозрачных пикселей цветовые каналы действительно нулевые, как требует премультиплицирование
      (ADR-0006). Заодно виден и topmost: бейдж нарисован поверх окна консоли (над развёрнутым и
      полноэкранным окном — отдельный пункт ниже, он этим кадром не покрыт).

      > **Чего этот пункт НЕ проверял до 2026-08-25 и почему пример пришлось править.** Тело
      > бейджа было зелёным (`B=0x20, G=0xC8, R=0x40`), а зелёный канал стоит **в середине** и
      > в BGRA, и в RGBA — то есть при перепутанном порядке байтов бейдж выглядел бы ровно так
      > же, и порядок из ADR-0006 глазами проверить было нельзя. Тело теперь синее с красной
      > полосой справа: синий и красный стоят по краям, поэтому перестановка каналов видна как
      > обмен цветов местами. Соответствующий пункт — ниже.
- [x] **порядок байтов BGRA подтверждён 2026-08-25:** тело нарисовано синим, полоса справа —
      красной, ровно как в исходнике (`[0xFF,0,0,0xFF]` = синий, `[0,0,0xFF,0xFF]` = красный).
      При чтении тех же байтов как RGBA тело было бы красным, а полоса синей — то есть проверка
      различающая, в отличие от прежнего зелёного бейджа (см. врезку выше);
- [ ] бейдж поверх **развёрнутого и полноэкранного** окна (поверх обычного окна консоли уже
      подтверждено скриншотом);
- [ ] якорь `Fixed` попадает в правый-нижний угол монитора **активного окна**, а не первичного.
      Проверять надо, **перетащив сам терминал** на не-первичный монитор, а не щёлкая чужое
      окно: нажатие Enter неизбежно делает активным окном консоль, поэтому шаг вида «щёлкните
      окно на другом экране, потом Enter» вернул бы фокус консоли и не проверил бы ничего.
      Пример печатает `chosen_monitor_dpi` — если у мониторов разный масштаб, это число само
      выдаёт, какой монитор выбрал адаптер;
- [x] **в простое (после `hide()`, процесс ещё жив) загрузка CPU нулевая — измерено 2026-08-25.**
      Прогон, оставленный на приглашении «press Enter to exit»: время жизни **280,7 с**,
      суммарно CPU **0,062 с**, то есть в среднем **0,022 %**; два потока (главный + насос
      оверлея), рабочий набор 8,2 МБ. Почти весь этот CPU израсходован на первые ~10 секунд
      анимации, дальше процесс ждёт на блокирующем чтении без единого таймера.

      Замер снят так (PID подставить свой):

      ```powershell
      $p = Get-Process -Name overlay_smoke
      [pscustomobject]@{
        Elapsed = ((Get-Date) - $p.StartTime).TotalSeconds
        Cpu     = $p.CPU
        Threads = $p.Threads.Count
      }
      ```

      Это и есть проверка ADR-0003 в измеримой форме: реализация с опросом давала бы ровную
      ненулевую загрузку пропорционально времени, а не константу в 60 миллисекунд.
- [ ] **смена масштаба в «Параметрах» при стоящем неподвижном бейдже** — путь, для которого
      `WM_DPICHANGED` единственный источник (бейдж не двигается, сравнивать нечего). Смотреть
      надо на **бейдж**, а не только на лог: он обязан сам переехать в правый-нижний угол
      новой рабочей области и, после ответа оболочки на `OverlayScaleChanged`, сменить размер.
      В логе при этом идут три строки подряд: «told the display configuration changed» →
      «re-placing the standing badge after a display change» → «asking for a re-render».

      > Первая редакция этого пункта просила только первую строку — и прошла бы, пока бейдж
      > стоит не на месте и в масштабе прошлого монитора: `rcWork` при смене масштаба
      > сдвигается (панель задач становится выше в физических пикселях), а бейдж остаётся по
      > старым координатам. Ровно этот дефект и был найден на ревью задачи 10: сообщение
      > ставило флаг, который читался только при следующей команде от оболочки, а её могло не
      > быть никогда — при якоре `Fixed` ядро выключает слежение за курсором, а режим Follow
      > отменяет таймер скрытия. Пункт, проверяющий наличие лога вместо наблюдаемого
      > поведения, — это пункт, который нельзя провалить.

## D. Курсор: слежение и нулевой простой (задача 11)

`cargo run -p switcher-windows --example overlay_smoke -- follow`
показывает бейдж у курсора. Первое Enter скрывает его и снимает Raw Input,
второе завершает процесс. Счётчик `pointer event rate` выводится по событиям
движения, без таймера в простое. Для подробного лога:
`$env:RUST_LOG = 'overlay_smoke=debug,switcher_windows=trace'`.

Автоматически проверено на Windows: при создании нет регистрации мыши;
после подтверждения `pointer_armed` Windows возвращает `RIDEV_INPUTSINK` и
ненулевую цель; после `pointer_disarmed` и после Drop регистрация отсутствует.
Это проверяет `GetRegisteredRawInputDevices`, а не внутренний флаг адаптера.
Нативный регрессионный тест также подтверждает, что дренаж `WM_INPUT` не
поглощает `WM_QUIT` и не отправляет движение после запроса завершения.

Ещё требует интерактивного прогона на пользовательском рабочем столе:

- [ ] плавное следование и фактическая частота событий при быстром движении;
- [ ] переход между мониторами с разным DPI без скачка размера/позиции;
- [ ] после `pointer_disarmed` при движении мыши нет новых `PointerMoved`;
- [ ] измерение CPU в скрытом состоянии нового режима `follow`.

Нативный тест регистрации не подтверждает эти визуальные и нагрузочные пункты.
Изолированная среда команд может не видеть активное окно пользовательского
рабочего стола: интерактивный прогон должен выполняться в пользовательской сессии.

## E. Фолбэк-опрос: три слоя наблюдаемости (задача 12)

Реализованы `layout_probe` и `layout_smoke`. Числовой аргумент ограничивает время
наблюдения в секундах; без аргумента `layout_probe` принимает Enter для снимка
через 3 секунды (успеть вернуть фокус целевому приложению), `q` завершает его.
`layout_smoke` печатает диагностический payload события и отдельный свежий
снимок `current()`, как требует ADR-0011.

2026-09-07, пользовательский рабочий стол Windows: `layout_probe 5` успешно
установил оба хука, прочитал `Chrome_WidgetWin_1`, HKL `0x04090409`, LANGID 1033.
Получены shell-сообщения 6 и 32774 на потоке-установщике: подписка не молчит.
В изолированной среде команд переднее окно было NULL; такой прогон не проверяет
пользовательский desktop. Смена языка во время этого замера не выполнялась.

Отдельный `layout_smoke 5` в пользовательской сессии прочитал `ru-RU` / HKL
`0x04190419`, подтвердил `foreground_hook_ready` и штатно завершился (`exit=0`).
Shell оставался `shell_delivery_unverified`, без ложного объявления проверки
переключений успешной. Полные гейты задачи 12: fmt и Clippy чистые, 103 теста прошли.

Автотесты проверяют лестницу классификации, ограниченные повторы снимка при
гонке фокуса, отказ от чтения нового окна в условном опросе, восстановление
после отказа пробуждения, прекращение публикации при отказе KillTimer,
прерывание backoff и отсутствие старого WM_QUIT после перезапуска.

- [ ] десять смен RU/EN в обычном Win32-приложении: источник и задержка <300 мс;
- [ ] отдельное подтверждение `HSHELL_LANGUAGE` (код 8); успешная регистрация
  и сообщения 6/32774 этого не доказывают;
- [ ] elevated, UWP, Terminal: фактические классы окон, читаемый HKL, доставка;
- [ ] `ForegroundPoll` отсутствует для обычного desktop-окна;
- [ ] по одному сообщению о взводе/снятии фолбэка; при снятии указан счётчик тиков;
- [ ] независимый замер контекстных переключений потока после снятия таймера.

### TSF — проверка 2026-09-07 (задача 13)

`layout_smoke` теперь поднимает все три подписки. На пользовательском desktop
45-секундный прогон успешно подписался на TSF и завершился с `exit=0`.
Переключения языка во время прогона не выполнялись: состояние оставалось
`tsf_delivery_unverified`, что не считается подтверждением глобальной доставки.
Локальные native-тесты проверили три цикла STA/Advise/Unadvise/Deactivate,
обработку S_FALSE и RPC_E_CHANGED_MODE, фильтрацию деактиваций и запрет IAgileObject.

- [ ] десять RU/EN-переключений в чужом Win32-процессе дают `source=Tsf`;
- [ ] payload callback и свежий foreground-снимок соответствуют переключению;
- [ ] совместная работа с shell/foreground не даёт лишних звуков/показов после
  подключения рантайма (задача 19).

Гейты задачи 13: fmt и Clippy чистые; 107 тестов прошли; windows-core в графе
зависимостей ровно одной версии — 0.62.2. Выбор sink и явный `Agile = false`
зафиксированы в [ADR-0013](../architecture/adr/0013-tsf-input-profile-activation-sink.md).

## F. Трей (задача 18)

Код реализован; native-тесты подтвердили straight alpha и обработку команд
после модального поглощения thread-wake. Это не заменяет UI-проверку:

- [ ] иконка читаема на 100/150/200%;
- [ ] клик «Звук» доставляет MenuCommand и сохраняет галку;
- [ ] открытое меню → смена языка → закрыть: иконка соответствует текущему языку;
- [ ] «Состояние» показывает отдельные причины ограничений;
- [ ] перезапуск Explorer восстанавливает иконку;
- [ ] «Выход» завершает все потоки и дописывает последнюю строку лога.

## G. Деградация возможностей (задачи 14, 16, 12)

Автозапуск: native roundtrip 2026-09-07 проверил REG_SZ с кавычками и UTF-16
терминатором, идемпотентность и удаление отдельного временного ключа вне Run.
`autostart_smoke` только читает настоящую запись. Автоматические проверки не
включали реальный автозапуск и не меняли его значение.

Звук: тесты проверяют частоты, ограничение громкости и 90-мс затухающий источник
по сэмплам, без открытия устройства. Для полного приложения остаётся:

- [ ] RU/EN/другой язык дают три различимых тона без щелчка в конце;
- [ ] volume=0 даёт тишину без ошибок;
- [ ] запуск без выходного устройства сохраняет enabled=true, показывает причину
  в трее и продолжает работу оверлея;
- [ ] потеря уже открытого устройства даёт audio_stream_failed один раз и
  не накапливает новые тоны в остановившемся микшере;
- [ ] отказ записи Run возвращает галку к перечитанному состоянию ОС;
- [ ] запуск при входе в Windows после включения из готового приложения.

## H. Конфиг: клампинг и предупреждения (задача 8)

Модель покрыта тестами switcher-core. В задаче 17 на Windows дополнительно
проверены создание и повторная замена файла с чтением обратно, отсутствие
оставшегося временного файла и защита повреждённого/более нового TOML от
последующих сохранений. Каталог вместо лога возвращает ошибку без паники.
Успешная установка глобального журнала и сброс его последних строк при выходе
подтверждены запуском полного приложения, см. раздел I.

## I. Полное приложение: запуск и завершение (задача 20)

Запуск с отдельными данными, без изменения обычного конфига:

```powershell
cargo build --release -p switcher-app
target\release\lang-switcher.exe --data-dir target\diagnostic --run-for 30
```

Обычный запуск без аргументов использует AppData. Файл конфига создаётся при
первом сохранении настройки, а не обязательно при старте. При необходимости
тишины заранее записать `[sound]` / `enabled = false` в диагностический config.toml.
Длительность `--run-for` задаёт отдельный одноразовый сигнал остановки и не
добавляет периодического опроса в обычный запуск. Release PE имеет subsystem
Windows; фактическое отсутствие всплывающей консоли ещё требует визуальной проверки.

| Пункт | Дата | Сборка | ОС и конфигурация мониторов | Результат | Заметки |
|---|---|---|---|---|---|
| Старт и штатный выход, 10 с | 2026-09-07 19:30 UTC | debug | Windows 11 Enterprise 10.0.26200; мониторы не исследовались | exit 0, процесс завершён | PMv2 из манифеста, Initial ru-RU, `tray ready`, `core loop stopped`, финальная строка `clean=true` |
| Свежий каталог, старт и выход, 75 с | 2026-09-07 19:43 UTC | release, 3 078 656 байт | Та же ОС, пользовательский desktop; конфигурация мониторов не фиксировалась | exit 0, stderr пуст | Initial en-US; строки PMv2, трей, shutdown присутствуют; ошибок нет |
| Shutdown во вложенном оконном цикле | 2026-09-07 | debug native-test | Windows; без взаимодействия с рабочим столом | PASS | Wndproc получает запрос внутри цикла без custom drain, WM_QUIT передан внешнему циклу; сам TrackPopupMenu не автоматизировался |
| Отрицательный DPI-контроль полного приложения | 2026-09-07 20:00 UTC | debug без манифеста | Windows 11, пользовательский desktop | exit 0; from_manifest=false, per_monitor_v2=true; WARN в файле | Предупреждение повторено после создания логгера; манифест возвращён в finally с обновлением mtime |
| Восстановленный манифест, финальная сборка | 2026-09-07 20:03 UTC | release, 3 081 216 байт | Та же ОС; мониторы не фиксировались | exit 0, stderr пуст, from_manifest=true, clean=true | Предупреждения о манифесте нет; это финальный бинарник после исправления диагностики |

Наблюдаемые ограничения — Caret=Off (M2), shell/TSF=Degraded до первого
подтверждённого события языка. Успешная регистрация не объявляется проверкой
глобальной доставки. В обоих прогонах звук отключён настройкой; устройство
открылось без ошибки. Прослушивания и переключений языка не было.

Артефакты локально: `target/m1-smoke-2026-09-07`,
`target/m1-release-smoke-2026-09-07/metrics.json` и подкаталоги `logs`.
`target` игнорируется Git; наблюдения в этой таблице сохранены независимо от него.

- [ ] В обычном запуске нажать «Выход» в трее: процесс исчезает, последняя строка
  `lang-switcher stopped clean=true` записана, бейдж и иконка исчезли.
- [ ] Запустить с `--run-for 15`, открыть и держать меню: срок закрывает меню и
  приложение без дополнительного клика. Это проверяет реальный TrackPopupMenu.

## Замер NFR (задача 21)

2026-09-07 проведён 620-секундный release-прогон, с 61 снимком ресурсов после
прогрева. Все условия, формулы и артефакты — в [отчёте о простое](../research/2026-09-07-idle-performance.md).

| Пункт | Дата | Сборка | ОС и конфигурация мониторов | Результат | Заметки |
|---|---|---|---|---|---|
| Бинарник | 2026-09-07 | release задачи 20 | Windows 11 Enterprise 10.0.26200; мониторы не влияют | 3 078 656 байт, в бюджете | Профиль не менялся |
| CPU скрытого бейджа | 2026-09-07 | release, sound=false | Та же ОС; foreground не фиксировался | 0,385% среднее, пик 10 с 1,243% | Первые 421,64 с; целевой порог не достигнут. Отдельно подтверждён расход cpal_wasapi_out |
| Память через 5 мин | 2026-09-07 | release | Та же ОС; мониторы не фиксировались | WorkingSet 20,09 МиБ, PrivateBytes 6,61 МиБ | Рабочий набор выше 5–15 МБ; после 20 переключений ещё не измерялось |
| Лог в простое | 2026-09-07 | release, INFO | Та же ОС | +0 байт за 421,64 с | За весь интервал +810 байт из-за трёх показов после седьмой минуты; чистые 10 минут ещё не подтверждены |
| Завершение длинного прогона | 2026-09-07 19:56 UTC | release | Пользовательский desktop | exit 0, stderr пуст, clean=true | Процесс после завершения отсутствует |
| Контекстные переключения layout-потока | — | — | — | не проверялось | Слой 3 раздела E |
| PointerMoved=0 при скрытом бейдже | — | — | — | ручной TRACE-прогон не проводился | Native-тест регистрации пройден, INFO-лог этот пункт не доказывает |

Во время длинного прогона после седьмой минуты появились три пары arm/disarm
указателя. Это подтверждает работу показов и скрытия в рантайме, но INFO-лог не
содержит источник раскладки; неизвестно, было ли это переключение языка в одном
окне или смена окон с разным HKL. Матрица доставки от этого не становится пройденной.

Независимое ревью выявило дополнительный дефект hover-таймера tray-icon 0.24.1;
аудио lifecycle и этот таймер остаются открытыми. Обоснование и следующий
проверяемый шаг для каждого — в отчёте о простое. Workflow
`rust-adversarial-review` недоступен; проведено read-only ревью независимым агентом.

## Итоговая матрица ручной приёмки

Запустить `layout_smoke` с ограничением времени или полное приложение с отдельным
каталогом и `LANG_SWITCHER_LOG=debug`. Для каждого целевого окна отдельно записать
его класс и наличие package identity: название приложения не доказывает категорию.
Один полный прогон начинается с 10 RU/EN в каждом классе и смены foreground
между двумя окнами с разными раскладками. По логу сверять свежий HKL/язык,
источник уведомления, отсутствие повторных показов и задержку <300 мс.

| Проверка | Действие и признак успеха | Статус на 2026-09-07 |
|---|---|---|
| Обычное непакетное Win32-окно | 10 RU/EN без смены фокуса; каждый переход отражён, фолбэк не взводится | не проверялось |
| Elevated-окно | 10 RU/EN; читаемый HKL и бейдж соответствуют окну, условный опрос виден в логе | не проверялось |
| Packaged/UWP-окно | Зафиксировать фактический класс, 10 RU/EN; источник и корректная раскладка наблюдаемы | не проверялось |
| Windows Terminal | 10 RU/EN; обновление не зависит от перехода в другое окно | не проверялось |
| Разные HKL у двух окон | Менять только фокус; индикатор следует раскладке нового foreground | не проверялось |
| Оверлей и DPI | Выполнить C/D: клик проходит, фокус сохраняется, topmost, края/Fixed и 100/150/200% корректны | исторические измерения 2026-08-25; новая визуальная приёмка открыта |
| Звук | Включить из трея; RU/EN различимы, без дублей/щелчка; выключить и снова включить без перезапуска | не проверялось |
| Меню, выход, Explorer | Выполнить F/I; настройки сохраняются, shutdown с открытым меню завершает процесс; восстановление иконки после перезапуска Explorer проверять отдельно | native-контракты пройдены; интерактивный прогон открыт |
| Автозапуск | После включения из трея следующий вход в Windows запускает бинарник; выключение снимает запись | изолированный registry roundtrip пройден; настоящий вход не проверялся |
| Отказы и конфиг | Выполнить G/H; причины видны в состоянии, предпочтения не теряются, защищённый TOML не перезаписывается | тесты на портах/native I/O пройдены; UI-проверка открыта |

Отказ записи Run проверяется тестовым портом/изолированным ключом: для этого
не нужно менять права реального Run пользователя. Приложение не должно обещать
успешный автозапуск по одному локальному roundtrip.

**M1 не закрыт.** Причины ограничений отображаются в трее; окна настроек ещё нет
(M2), поэтому требование «в настройках/трее» реализовано только в части трея.
Приёмка ждёт ручной матрицы и устранения/согласованного пересмотра NFR.
