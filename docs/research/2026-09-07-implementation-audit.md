# Аудит реализации lang-switcher — 2026-09-07

Проверена рабочая копия `.claude/worktrees/m1-plan-adrs`, ветка `feat/m1-task10-overlay`,
исходный коммит `d855bea`. Основная папка проекта находится на более раннем `af4be79`;
новейшие изменения нельзя оценивать только по её файлам. Правки аудита оставлены в
актуальной рабочей копии, без слияния веток и без коммита.

**Продолжение 2026-09-07:** по указанию владельца актуальные коммиты и все исправления
перенесены в основную папку проекта. Совпадение файлов при переносе проверено побайтово;
дальнейшая разработка ведётся там, без worktree.

## Вывод

Назначение понятно: резидентный индикатор текущей раскладки RU/EN возле курсора,
со звуком, треем и автозапуском; Windows 11 сначала. Разделение на независимое ядро,
порты и нативные адаптеры пригодно для продолжения, переписывать проект целиком оснований нет.
Однако это ещё не MVP: выполнены задачи 1–10 из 21, основной бинарник проверяет
DPI-awareness и завершается. Проверка ядра не заменяет проверку приложения в работе.

## Найдено и исправлено

| Приоритет | Дефект и воспроизведение | Исправление |
|---|---|---|
| Высокий | EN → RU → EN быстрее 150 мс отбрасывал последнее событие; без новых событий индикатор оставался RU | Удалена временная эвристика, сохранён дедуп текущего HKL. Для будущего рантайма принят ADR-0011: подтверждать раскладку чтением `current()` при обработке уведомления |
| Средний | Сохранённый Follow + Initial оставлял бейдж скрытым до следующей смены языка | Начальное чтение восстанавливает Follow через QueryAnchor, без звука и таймера скрытия |
| Средний | Цвет `ru-RU` сохранялся как `ru-ru`, но поиск шёл по `ru` | Нормализация через primary subtag; уже заданный канонический ключ имеет приоритет |
| Средний | Интерактивный smoke блокировался на stdin и не обрабатывал смену DPI до Enter | Блокирующий `select!` между Enter и событиями; никаких периодических таймеров. Регрессионный тест включён в обычный `cargo test --workspace` |
| Средний | Изменение только рабочей области экрана не передвигало неподвижный бейдж | `WM_SETTINGCHANGE` использует существующий отложенный путь обновления геометрии, безопасный при реентрантном wndproc |
| Средний | Невалидное новое изображение или ошибка блита могли оставить видимым старый язык и рассогласовать размер Surface/окна | Ошибка показа скрывает бейдж, сбрасывает Surface/якорь, сообщает Degraded; успешный последующий показ сообщает Ok. Скрытое окно не перемещается |
| Документация | README утверждал, что адаптеров нет и main пустой; smoke описывал уже отсутствующие паузы | Описаны текущая реализация, способ запуска прототипа и открытые ручные проверки |

Изменение дедупликации зафиксировано в
[ADR-0011](../architecture/adr/0011-authoritative-layout-snapshots.md).
**Перечитывание раскладки рантаймом пока не реализовано:** это обязательная часть задач
12 и 19, а не проверенная end-to-end функция. Для TSF необходимо подтвердить, что после
его уведомления снимок раскладки переднего окна отражает нужное изменение.

## Исправления плана дальнейшей работы

- TSF: `RPC_E_CHANGED_MODE` нельзя считать успешным созданием STA. `CoUninitialize`
  вызывается только за успешный `CoInitializeEx`, включая `S_FALSE`, после освобождения
  интерфейсов на том же потоке. Уточнено по
  [Microsoft CoInitializeEx](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-coinitializeex).
- Рантайм: проверять абсолютный дедлайн скрытия перед каждым `select!`, чтобы
  непрерывный поток движений мыши не откладывал скрытие навсегда.
- Запуск: начальное чтение и эффекты `Initial` выполняются до обработки очереди хуков;
  не ставить Initial в хвост уже работающих источников.
- Выход: нужен отдельный сигнал остановки; копии `Sender` у адаптеров не позволяют
  считать канал закрытым после удаления только main-копии. Добавлен сценарий приёмки.
- Автозапуск: после отказа записи перечитать реестр, который мог измениться извне;
  при отказе чтения показывать неизвестное состояние, не выдавая старый конфиг за факт ОС.
- Добавлены требования к регрессионным тестам этих сценариев в задаче 19. Старые
  листинги задачи 5 с ошибочной временной эвристикой удалены.

Обновление геометрии при системных настройках соответствует
[WM_SETTINGCHANGE](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-settingchange).
Проверялись также контракты
[UpdateLayeredWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-updatelayeredwindow)
и [PostThreadMessageW](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-postthreadmessagew).

## Доказательства и ограничения

На Windows, Rust 1.96.1:

- `cargo fmt --all -- --check` — exit 0.
- `cargo clippy --workspace --all-targets --locked -- -D warnings` — exit 0, предупреждений нет.
- `cargo test --workspace --locked` — **87 passed, 0 failed**: core 57, platform 3,
  Windows 26, smoke-пример 1. Doc-tests также завершились успешно.
- Для исправлений сначала наблюдались падения регрессионных тестов, затем успешные прогоны.
- `cargo build --workspace --locked` — exit 0.
- `cargo +1.87.0 check --workspace --all-targets --locked` до и после исправлений — exit 0;
  проверка установленного MSRV потребовала запуска вне песочницы из-за Access is denied.
- `RUST_LOG=debug cargo run -p switcher-app --locked` — exit 0,
  `from_manifest=true per_monitor_v2=true`. Это проверка манифеста, не рабочего приложения.

При проверке обработчика ошибок реально воспроизведён отказ из-за неверного буфера;
отказ GDI/UpdateLayeredWindow не инъецировался. Для него проверен код общего пути очистки.
Тест `WM_SETTINGCHANGE` проверяет реакцию обработчика, не меняет настройки пользователя.

Ручные click-through, отсутствие кражи фокуса, topmost, изменение панели задач,
визуальный mixed-DPI и NFR готового приложения повторно **не проверялись**.
Linux/macOS и удалённый CI в этом аудите не запускались. Ранее записанные ручные
наблюдения 2026-08-25 сохранены отдельно в [чеклисте](../smoke/m1-windows.md).

## Продолжение

Следующий шаг — задача 11 (Raw Input только при видимом бейдже), затем задачи 12–13
для проверки детекции в обычном, повышенном, UWP-окне и терминале. После снятия этих
рисков — автозапуск, растеризация, звук, конфиг/логи, трей и общий рантайм. Задача 21
завершает приёмку с ручными проверками и измерениями ресурсов.
